<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Promise to You 🌙</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Outfit:wght@300;600&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0; height: 100vh; background: #020617;
            display: flex; justify-content: center; align-items: center;
            color: white; text-align: center; overflow: hidden;
            font-family: 'Outfit', sans-serif;
        }

        canvas { position: absolute; top: 0; left: 0; z-index: 1; }

        .content {
            position: relative; z-index: 10; padding: 20px;
            background: rgba(2, 6, 23, 0.4);
            backdrop-filter: blur(8px);
            border-radius: 30px;
            max-width: 600px;
        }

        h1 {
            font-family: 'Dancing Script', cursive;
            font-size: 4.5rem; color: #38bdf8;
            margin: 0; text-shadow: 0 0 20px rgba(56, 189, 248, 0.6);
        }

        .typewriter {
            font-size: 1.4rem; line-height: 1.6; color: #cbd5e1;
            margin-top: 20px; min-height: 100px;
        }

        .highlight {
            color: #f472b6; font-weight: 600;
            text-shadow: 0 0 10px rgba(244, 114, 182, 0.5);
        }

        /* Floating particles for a magical feel */
        .sparkle {
            position: absolute; background: white; border-radius: 50%;
            pointer-events: none; opacity: 0.5;
        }
    </style>
</head>
<body>

    <canvas id="nebulaCanvas"></canvas>

    <div class="content">
        <h1 id="title">To the Moon...</h1>
        <div class="typewriter" id="typedText"></div>
    </div>

    <script>
        // --- 1. THE NEBULA BACKGROUND ---
        const canvas = document.getElementById('nebulaCanvas');
        const ctx = canvas.getContext('2d');
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        let stars = [];
        for (let i = 0; i < 200; i++) {
            stars.push({
                x: Math.random() * canvas.width,
                y: Math.random() * canvas.height,
                size: Math.random() * 2,
                opacity: Math.random(),
                speed: Math.random() * 0.005 + 0.002
            });
        }

        function drawStars() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            stars.forEach(s => {
                s.opacity += s.speed;
                if (s.opacity > 1 || s.opacity < 0) s.speed *= -1;
                ctx.fillStyle = `rgba(255, 255, 255, ${s.opacity})`;
                ctx.beginPath();
                ctx.arc(s.x, s.y, s.size, 0, Math.PI * 2);
                ctx.fill();
            });
            requestAnimationFrame(drawStars);
        }
        drawStars();

        // --- 2. THE TYPEWRITER MESSAGE ---
        const message = "I know I'm missing this birthday celebration right now, and it's the hardest part of my day. But here is my vow: The birthday we finally celebrate together will rewrite every memory. I promise you, it will be unforgettable. ✨";
        let index = 0;
        const speed = 50; // Speed of typing in ms

        function typeWriter() {
            if (index < message.length) {
                let char = message.charAt(index);
                // Custom highlighting for specific words
                if (message.substring(index, index + 8) === "together") {
                    document.getElementById("typedText").innerHTML += '<span class="highlight">together</span>';
                    index += 8;
                } else if (message.substring(index, index + 13) === "unforgettable") {
                    document.getElementById("typedText").innerHTML += '<span class="highlight">unforgettable</span>';
                    index += 13;
                } else {
                    document.getElementById("typedText").innerHTML += char;
                    index++;
                }
                setTimeout(typeWriter, speed);
            }
        }

        // Start typing after a short delay
        setTimeout(typeWriter, 1000);
    </script>
</body>
</html>
